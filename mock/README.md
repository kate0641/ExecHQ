# Mock data

Local JSON only. If a placeholder page ever needs data to show a layout, it goes
here and is imported directly.

Nothing in this prototype talks to a network — no fetch, no API routes, no
database. If a screen appears to need real data, that is a conversation, not a
file.
